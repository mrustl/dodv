##############################################################################
### Autogenerated with R package kwb.pkgbuild v0.1.1
### (installed from 'Github (kwb-r/kwb.pkgbuild@0ac3694)' source code on 2019-09-09)
### by calling the function kwb.pkgbuild::use_autopkgdown("dodv", org = "mrustl")
### (file created at: 2019-09-24 08:55:21)
##############################################################################


language: r
sudo: required
services:
- docker
cache: packages
r_packages:
- remotes
- covr
before_install:
- svn checkout https://github.com/SeleniumHQ/selenium/trunk/common/src/web
- docker run -d -p 3000:8080 --name http-server -v $(pwd)/web:/public danjellz/http-server &
- docker run -d -p 127.0.0.1:4444:4444 -v /dev/shm:/dev/shm --link http-server selenium/standalone-firefox:3.141.59
matrix:
  include:
  - r: devel
  - r: release
    after_success:
    - Rscript -e 'covr::codecov()'
    before_deploy:
    - Rscript -e 'remotes::install_cran("pkgdown")'
    deploy:
      provider: script
      script: Rscript -e 'pkgdown::deploy_site_github(verbose = TRUE)'
      skip_cleanup: 'true'
      on:
        branch: 
        - master
        - dev
  - r: oldrel
